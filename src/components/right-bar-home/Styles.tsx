import { style } from "typestyle";

const Styles = {
  container: style({
    border: 2,
  }),

  headerText: style({
    marginTop: 10,
    fontSize: 14,
    fontWeight: "bold",
    textAlign: "center",
  }),

  formLabel: style({
    fontSize: 13,
  }),

  formInput: style({
    height: 25,
    fontSize: 13,
  }),

  button: style({
    marginTop: 20,
    marginBottom: 5,
  }),
};

export default Styles;
