import { style } from "typestyle";

const Styles = {
  container: style({
    height: 55,
  }),

  label: style({
    fontSize: 13,
    fontWeight: "bold",
  }),

  value: style({
    fontSize: 13,
  }),
};

export default Styles;
