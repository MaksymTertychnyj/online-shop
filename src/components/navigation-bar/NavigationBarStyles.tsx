import { style } from "typestyle";

const NavigationBarStyles = {
  image: style({
    width: 30,
    height: 30,
    borderRadius: 5,
  }),

  bagButton: style({
    width: 35,
  }),

  bagPrice: style({
    fontSize: 10,
    color: "red",
  }),
};

export default NavigationBarStyles;
